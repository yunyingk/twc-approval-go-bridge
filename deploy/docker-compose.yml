services:
  server:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    environment:
      HTTP_ADDR: :8080
      LOG_LEVEL: info
      SHUTDOWN_TIMEOUT: 10s
    ports:
      - "8080:8080"
